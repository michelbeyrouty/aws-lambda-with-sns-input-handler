{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Program",
            "program": "${workspaceFolder}/debug/trigger-lambda.js",
            "outFiles": [
                "${workspaceFolder}/**/*.js"
            ]
        }
    ]
}